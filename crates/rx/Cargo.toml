[package]
name = "rx"
version = "0.1.0"
edition = "2024"

[dependencies]
esp-hal = { workspace = true, features = ["esp32c3", "rt", "log-04"] }
esp-println = { workspace = true, features = ["esp32c3", "log-04", "auto" ]}
log = { workspace = true }
